<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.MyApplication"
        tools:targetApi="35"
        android:usesCleartextTraffic="true"

        >
        <activity android:name=".activities.SigninActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity"
            android:exported="true"/>
        <activity android:name=".activities.CharactersActivity"
            android:exported="false"/>
        <activity android:name=".activities.CharacterEditorActivity"
            android:exported="false"/>
        <activity android:name=".activities.CampaignsActivity"
            android:exported="false"/>
        <activity android:name=".activities.LocationsActivity"
            android:exported="false"/>
        <activity android:name=".activities.QuestsActivity"
            android:exported="false"/>
        <activity android:name=".activities.NPCsActivity"
            android:exported="false"/>

    </application>

</manifest>